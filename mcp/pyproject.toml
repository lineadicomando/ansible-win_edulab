[project]
name = "win-edulab-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
    "pyyaml>=6.0",
]

[project.scripts]
win-edulab-mcp = "server:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
